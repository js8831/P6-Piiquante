// Importation du modele sauce pour l'utiliser dans les middlewares qu'on affectera aux routes
// La majuscule a-t-elle un impact ? Comme je l'ai nommé Sauce
const Sauce = require("../models/sauce");
